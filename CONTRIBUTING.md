# Contributing

Changes are ADDITIVE; do not force-push to `main`. The deterministic vectors in `testdata/vectors.json` are
the cross-language contract — all three implementations must agree.

Authored by Yachay <yachay@szlholdings.dev>. Co-Authored-By: Perplexity Computer Agent.
