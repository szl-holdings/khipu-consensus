{
  "name": "@szl-holdings/khipu-consensus",
  "version": "0.1.0",
  "description": "Byzantine-fault-tolerant multi-party signed agreement (3-of-4 BFT). Each witness signs an action hash with its own ECDSA-P256 key over the DSSE PAE.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx test/vectors.test.ts"
  },
  "author": "Yachay <yachay@szlholdings.dev>",
  "engines": { "node": ">=18" }
}
